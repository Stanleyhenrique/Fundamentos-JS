const [a,b,c,d] = [1,2,3,4]

const soma = a + b + c + d // 10
const subtracao = d - a // 3
const multiplicacao = a * d // 4
const divisao = c / a // 3
const modulo = a % 2 // 1

console.log(soma, subtracao, multiplicacao, divisao, modulo)